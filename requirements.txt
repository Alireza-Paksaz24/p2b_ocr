fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
aiofiles>=23.2.1
python-dotenv>=1.0.1
pyyaml>=6.0.1
sqlalchemy>=2.0.30
pillow>=10.3.0
pdf2image>=1.17.0
python-docx>=1.2.0
markdown2>=2.4.13
huggingface_hub>=0.23.0
tqdm>=4.66.4
transformers>=4.46.0,<5.0.0
torch>=2.2.0
torchvision>=0.17.0

# DeepSeek OCR / OCR-2 dependencies
addict
matplotlib
requests
einops
easydict

# Florence-2 / general vision deps
timm>=0.9.0

# Flash Attention 2 — dramatically reduces VRAM (compile takes ~5 min on first install)
# pip install flash-attn --no-build-isolation
# flash-attn>=2.5.0

# Surya OCR — manages its own model weights
# pip install surya-ocr

# Optional Tesseract fallback
# pytesseract>=0.3.10